{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default app capability set.",
  "windows": ["main"],
  "permissions": [
    "core:window:allow-outer-position",
    "core:window:allow-set-position"
  ]
}
